import { CommonPipesPipe } from './common-pipes.pipe';

describe('CommonPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CommonPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
